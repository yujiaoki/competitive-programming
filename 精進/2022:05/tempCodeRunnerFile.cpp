bool ok = false;
    // if ((S.size() == T.size() && S.at(0).size() == T.at(0).size())
    //   || (S.size() == T.at(0).size() && S.at(0).size() == T.size())) {
    //         rep(_,4) {
    //             T = rotate(T);
    //             if (checker(S,T)) ok = true;
    //         }
    //   }
    
    // if (ok) cout << "Yes" << endl;
    // else cout << "No" << endl;